(function () {
  'use strict';

  const ADMIN_EMAIL = 'ab.m.dent.clinic@gmail.com';
  const state = { lang: localStorage.getItem('admin_lang') || 'en', client: null, user: null, section: 'overview', cache: {}, config: null };
  const translations = {
    en: { adminLabel: 'CLINIC ADMINISTRATION', welcomeBack: 'Welcome back', loginIntro: 'Sign in to manage your clinic workspace.', email: 'Email', password: 'Password', signIn: 'Sign in', clinicAdmin: 'Clinic Admin', overview: 'Overview', appointments: 'Appointments', patients: 'Patients', services: 'Services', gallery: 'Gallery', doctorProfile: 'Doctor profile', clinicSettings: 'Clinic settings', messages: 'Messages', logout: 'Log out', privateWorkspace: 'PRIVATE WORKSPACE', configurationRequired: 'Configuration required', configurationHelp: 'The local Supabase configuration could not be loaded. Confirm that the root .env.local file is available to your local web server.', loading: 'Loading secure workspace...', todayAppointments: "Today's appointments", pendingAppointments: 'Pending appointments', confirmedAppointments: 'Confirmed appointments', completedAppointments: 'Completed appointments', cancelledAppointments: 'Cancelled appointments', totalPatients: 'Total patients', newMessages: 'New messages', recentAppointments: 'Recent appointments', noAppointments: 'No appointments yet', ready: 'Your workspace is ready for data.', add: 'Add', edit: 'Edit', delete: 'Delete', save: 'Save changes', cancel: 'Cancel', search: 'Search', status: 'Status', date: 'Date', actions: 'Actions', details: 'Details', archive: 'Archive', name: 'Name', phone: 'Phone', service: 'Service', notes: 'Notes', time: 'Time', latest: 'Latest', new: 'New', pending: 'Pending', confirmed: 'Confirmed', completed: 'Completed', cancelled: 'Cancelled', active: 'Active', inactive: 'Inactive', noRecords: 'No records found.', loadingData: 'Loading data...', confirmDelete: 'Delete this record permanently?', saved: 'Changes saved.', deleted: 'Record deleted.', error: 'Something went wrong.', invalid: 'Invalid', invalidImage: 'Use a JPG, PNG, or WebP image up to 5 MB.', storageError: 'Image upload failed. Apply storage policies and try again.', loginFailed: 'Unable to sign in. Check your email and password.', accessDenied: 'This account is not authorized for the clinic dashboard.', signedOut: 'You have been signed out.', biography: 'Biography', qualifications: 'Qualifications', fellowship: 'Fellowship', image: 'Image', category: 'Category', title: 'Title', price: 'Price', duration: 'Duration (minutes)', address: 'Address', workingHours: 'Working hours', instagram: 'Instagram', googleMaps: 'Google Maps URL', message: 'Message', read: 'Read', unread: 'Unread', markRead: 'Mark read', markUnread: 'Mark unread', clinicName: 'Clinic name', upload: 'Upload image', storageSetup: 'Storage policies must be applied in Supabase SQL Editor.', selectSection: 'Select a section from the sidebar.' },
    ar: { adminLabel: 'إدارة العيادة', welcomeBack: 'مرحبًا بعودتك', loginIntro: 'سجّل الدخول لإدارة مساحة عيادتك.', email: 'البريد الإلكتروني', password: 'كلمة المرور', signIn: 'تسجيل الدخول', clinicAdmin: 'إدارة العيادة', overview: 'نظرة عامة', appointments: 'المواعيد', patients: 'المرضى', services: 'الخدمات', gallery: 'المعرض', doctorProfile: 'ملف الدكتور', clinicSettings: 'إعدادات العيادة', messages: 'الرسائل', logout: 'تسجيل الخروج', privateWorkspace: 'مساحة خاصة', configurationRequired: 'الإعداد مطلوب', configurationHelp: 'تعذر تحميل إعدادات Supabase المحلية. تأكد من أن ملف .env.local متاح لخادم الويب المحلي.', loading: 'جارٍ تحميل المساحة الآمنة...', todayAppointments: 'مواعيد اليوم', pendingAppointments: 'المواعيد المعلقة', confirmedAppointments: 'المواعيد المؤكدة', completedAppointments: 'المواعيد المكتملة', cancelledAppointments: 'المواعيد الملغاة', totalPatients: 'إجمالي المرضى', newMessages: 'الرسائل الجديدة', recentAppointments: 'أحدث المواعيد', noAppointments: 'لا توجد مواعيد بعد', ready: 'مساحتك جاهزة للبيانات.', add: 'إضافة', edit: 'تعديل', delete: 'حذف', save: 'حفظ التغييرات', cancel: 'إلغاء', search: 'بحث', status: 'الحالة', date: 'التاريخ', actions: 'الإجراءات', details: 'التفاصيل', archive: 'أرشفة', name: 'الاسم', phone: 'الهاتف', service: 'الخدمة', notes: 'ملاحظات', time: 'الوقت', latest: 'الأحدث', new: 'جديد', pending: 'معلق', confirmed: 'مؤكد', completed: 'مكتمل', cancelled: 'ملغى', active: 'نشط', inactive: 'غير نشط', noRecords: 'لا توجد سجلات.', loadingData: 'جارٍ تحميل البيانات...', confirmDelete: 'هل تريد حذف هذا السجل نهائيًا؟', saved: 'تم حفظ التغييرات.', deleted: 'تم حذف السجل.', error: 'حدث خطأ.', invalid: 'غير صالح', invalidImage: 'استخدم صورة JPG أو PNG أو WebP بحجم أقصى 5 ميجابايت.', storageError: 'فشل رفع الصورة. طبّق سياسات التخزين ثم حاول مرة أخرى.', loginFailed: 'تعذر تسجيل الدخول. تحقق من البريد وكلمة المرور.', accessDenied: 'هذا الحساب غير مصرح له بدخول لوحة العيادة.', signedOut: 'تم تسجيل الخروج.', biography: 'السيرة الذاتية', qualifications: 'المؤهلات', fellowship: 'الزمالة', image: 'الصورة', category: 'التصنيف', title: 'العنوان', price: 'السعر', duration: 'المدة (بالدقائق)', address: 'العنوان', workingHours: 'ساعات العمل', instagram: 'إنستجرام', googleMaps: 'رابط خرائط جوجل', message: 'الرسالة', read: 'مقروءة', unread: 'غير مقروءة', markRead: 'تحديد كمقروءة', markUnread: 'تحديد كغير مقروءة', clinicName: 'اسم العيادة', upload: 'رفع صورة', storageSetup: 'يجب تطبيق سياسات التخزين من محرر SQL في Supabase.', selectSection: 'اختر قسمًا من القائمة الجانبية.' }
  };
  const $ = (selector) => document.querySelector(selector);
  const $$ = (selector) => Array.from(document.querySelectorAll(selector));
  const text = (key) => translations[state.lang][key] || key;
  const esc = (value) => String(value ?? '').replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[char]));
  const field = (label, name, value = '', type = 'text', required = false) => `<label>${esc(label)}<input name="${name}" type="${type}" value="${esc(value)}" ${required ? 'required' : ''}></label>`;
  const area = (label, name, value = '') => `<label>${esc(label)}<textarea name="${name}" rows="3">${esc(value)}</textarea></label>`;
  const imagePreview = (path) => { if (!path) return ''; const source = path.startsWith('assets/') ? `../${path}` : path; if (!/^https?:\/\//.test(source) && !source.startsWith('../assets/')) return ''; return `<img class="image-preview" src="${esc(source)}" alt="" loading="lazy">`; };

  function applyLanguage() { document.documentElement.lang = state.lang; document.documentElement.dir = state.lang === 'ar' ? 'rtl' : 'ltr'; $$('[data-i18n]').forEach((element) => { element.textContent = text(element.dataset.i18n); }); $$('[data-language-toggle]').forEach((button) => { button.textContent = state.lang === 'ar' ? 'EN' : 'العربية'; }); }
  function show(view) { ['loading-view', 'login-view', 'dashboard-view', 'configuration-view'].forEach((id) => { const element = $('#' + id); if (element) element.hidden = id !== view; }); }
  function toast(message, error = false) { const element = $('#toast'); element.textContent = message; element.classList.toggle('error', error); element.classList.add('show'); window.setTimeout(() => element.classList.remove('show'), 3500); }
  function busy(isBusy) { $('#dashboard-content').classList.toggle('is-loading', isBusy); }
  function normalizeError(error) { return error?.message || text('error'); }

  function loadEnvironment() { const config = window.ADMIN_SUPABASE_CONFIG; if (!config?.url || !config?.publishableKey?.startsWith('sb_publishable_')) throw new Error('Invalid publishable configuration'); return { VITE_SUPABASE_URL: config.url, VITE_SUPABASE_PUBLISHABLE_KEY: config.publishableKey }; }
  async function query(table, options = {}) { let request = state.client.from(table).select(options.select || '*'); if (options.order) request = request.order(options.order, { ascending: options.ascending ?? false }); if (options.limit) request = request.limit(options.limit); if (options.filters) Object.entries(options.filters).forEach(([key, value]) => { request = request.eq(key, value); }); const result = await request; if (result.error) throw result.error; return result.data || []; }
  async function count(table, filters = {}) { let request = state.client.from(table).select('*', { count: 'exact', head: true }); Object.entries(filters).forEach(([key, value]) => { request = request.eq(key, value); }); const result = await request; if (result.error) throw result.error; return result.count || 0; }
  async function save(table, id, values) { const result = id ? await state.client.from(table).update(values).eq('id', id).select().single() : await state.client.from(table).insert(values).select().single(); if (result.error) throw result.error; return result.data; }
  async function removeMedia(path) { if (!path || !path.startsWith('clinic-media/')) return; const result = await state.client.storage.from('clinic-media').remove([path.replace(/^clinic-media\//, '')]); if (result.error) throw result.error; }
  async function remove(table, id) { const current = state.cache[table]?.find((item) => item.id === id); const result = await state.client.from(table).delete().eq('id', id); if (result.error) throw result.error; if (current && ['gallery', 'services', 'doctor_profile'].includes(table)) await removeMedia(current.image_path || current.image_url); }
  function validateValue(name, value) { if (['phone', 'whatsapp'].includes(name) && value && !/^\+?[0-9 ()-]{7,20}$/.test(value)) throw new Error(`${text('invalid')} ${text(name)}`); if (['email'].includes(name) && value && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)) throw new Error(`${text('invalid')} ${text(name)}`); if (name.endsWith('_url') && value) { try { const url = new URL(value); if (!['http:', 'https:'].includes(url.protocol)) throw new Error(); } catch { throw new Error(`${text('invalid')} URL`); } } }
  function validateValues(values) { Object.entries(values).forEach(([name, value]) => validateValue(name, value)); if (values.preferred_date && Number.isNaN(Date.parse(values.preferred_date))) throw new Error(`${text('invalid')} ${text('date')}`); if (values.preferred_time && !/^([01]\d|2[0-3]):[0-5]\d$/.test(values.preferred_time)) throw new Error(`${text('invalid')} ${text('time')}`); }
  async function uploadMedia(file, folder) { if (!file || !file.size) return null; if (!['image/jpeg', 'image/png', 'image/webp'].includes(file.type) || file.size > 5 * 1024 * 1024) throw new Error(text('invalidImage')); const safeName = file.name.toLowerCase().replace(/[^a-z0-9._-]/g, '-'); const path = `${folder}/${crypto.randomUUID()}-${safeName}`; const { data: sessionData } = await state.client.auth.getSession(); const xhr = new XMLHttpRequest(); const result = await new Promise((resolve, reject) => { xhr.open('POST', `${state.config.url}/storage/v1/object/clinic-media/${path}`); xhr.setRequestHeader('Authorization', `Bearer ${sessionData.session.access_token}`); xhr.setRequestHeader('apikey', state.config.publishableKey); xhr.setRequestHeader('Content-Type', file.type); xhr.upload.onprogress = (event) => { if (event.lengthComputable) toast(`${text('upload')} ${Math.round((event.loaded / event.total) * 100)}%`); }; xhr.onload = () => xhr.status >= 200 && xhr.status < 300 ? resolve(true) : reject(new Error(text('storageError'))); xhr.onerror = () => reject(new Error(text('storageError'))); xhr.send(file); }); return result ? path : null; }
  function mediaField(label) { return `<label>${esc(label)}<input name="image_file" type="file" accept="image/jpeg,image/png,image/webp"></label>`; }

  function renderShell(title, body, action = '') { $('#page-title').textContent = title; $('#dashboard-content').innerHTML = `<div class="section-heading"><div><p class="eyebrow">${state.lang === 'ar' ? 'مساحة الإدارة' : 'Management workspace'}</p><h2>${esc(title)}</h2></div>${action}</div>${body}`; }
  function loadingView() { $('#dashboard-content').innerHTML = `<div class="loading-inline">${text('loadingData')}</div>`; }
  function emptyView() { return `<div class="empty-state"><div><strong>${text('noRecords')}</strong><span>${text('selectSection')}</span></div></div>`; }
  function statusBadge(status) { return `<span class="status status-${esc(status)}">${esc(text(status))}</span>`; }
  function tableView(headers, rows) { return `<div class="table-wrap"><table><thead><tr>${headers.map((header) => `<th>${esc(header)}</th>`).join('')}</tr></thead><tbody>${rows || `<tr><td colspan="${headers.length}">${emptyView()}</td></tr>`}</tbody></table></div>`; }
  function modal(title, body, submitLabel = text('save')) { const container = document.createElement('div'); container.className = 'modal-backdrop'; container.innerHTML = `<section class="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title"><div class="modal-head"><h2 id="modal-title">${esc(title)}</h2><button type="button" class="modal-close" data-close-modal aria-label="${esc(text('cancel'))}">×</button></div><form id="modal-form" class="modal-form">${body}<div class="modal-actions"><button type="button" class="button button-quiet" data-close-modal>${esc(text('cancel'))}</button><button type="submit" class="button button-primary">${esc(submitLabel)}</button></div></form></section>`; document.body.appendChild(container); container.querySelectorAll('[data-close-modal]').forEach((button) => button.addEventListener('click', () => container.remove())); return container; }

  async function renderOverview() { busy(true); try { const today = new Date().toISOString().slice(0, 10); const [todayCount, pending, confirmed, completed, cancelled, patients, messages, recent] = await Promise.all([count('appointments', { preferred_date: today }), count('appointments', { status: 'new' }), count('appointments', { status: 'confirmed' }), count('appointments', { status: 'completed' }), count('appointments', { status: 'cancelled' }), count('patients'), count('contact_messages', { status: 'unread' }), query('appointments', { order: 'created_at', limit: 5 })]); const rows = recent.map((item) => `<tr><td>${esc(item.patient_name)}</td><td>${esc(item.service || '-')}</td><td>${esc(item.preferred_date || '-')}</td><td>${statusBadge(item.status)}</td></tr>`).join(''); renderShell(text('overview'), `<section class="stats-grid"><article class="stat-card"><p>${text('todayAppointments')}</p><strong>${todayCount}</strong><small>${esc(today)}</small></article><article class="stat-card"><p>${text('pendingAppointments')}</p><strong>${pending}</strong><small>${text('new')}</small></article><article class="stat-card"><p>${text('confirmedAppointments')}</p><strong>${confirmed}</strong><small>${text('confirmed')}</small></article><article class="stat-card"><p>${text('completedAppointments')}</p><strong>${completed}</strong><small>${text('completed')}</small></article><article class="stat-card"><p>${text('cancelledAppointments')}</p><strong>${cancelled}</strong><small>${text('cancelled')}</small></article><article class="stat-card"><p>${text('totalPatients')}</p><strong>${patients}</strong><small>${text('patients')}</small></article><article class="stat-card"><p>${text('newMessages')}</p><strong>${messages}</strong><small>${text('messages')}</small></article></section><section class="panel full-panel"><div class="panel-title"><h3>${text('recentAppointments')}</h3><button class="button button-quiet" data-go="appointments">${text('details')}</button></div>${tableView([text('name'), text('service'), text('date'), text('status')], rows)}</section>`); $$('[data-go]').forEach((button) => button.addEventListener('click', () => navigate(button.dataset.go))); } catch (error) { renderShell(text('overview'), `<div class="error-state">${esc(normalizeError(error))}</div>`); } finally { busy(false); } }

  async function renderAppointments() { loadingView(); try { const data = await query('appointments', { order: 'preferred_date' }); state.cache.appointments = data; const rows = data.map((item) => `<tr><td>${esc(item.patient_name)}</td><td>${esc(item.phone)}</td><td>${esc(item.service || '-')}</td><td>${esc(item.preferred_date || '-')} ${esc(item.preferred_time || '')}</td><td>${statusBadge(item.status)}</td><td><div class="row-actions"><button class="small-button" data-edit-appointment="${item.id}">${text('details')}</button><button class="small-button danger" data-delete="appointments" data-id="${item.id}">${text('delete')}</button></div></td></tr>`).join(''); renderShell(text('appointments'), `<div class="toolbar"><input id="module-search" placeholder="${esc(text('search'))}" aria-label="${esc(text('search'))}"><select id="status-filter" aria-label="${esc(text('status'))}"><option value="">${text('status')}</option><option value="new">${text('new')}</option><option value="pending">${text('pending')}</option><option value="confirmed">${text('confirmed')}</option><option value="completed">${text('completed')}</option><option value="cancelled">${text('cancelled')}</option></select><input id="date-filter" type="date" aria-label="${esc(text('date'))}"><button class="button button-primary" id="add-appointment">+ ${text('add')}</button></div><div id="module-table">${tableView([text('name'), text('phone'), text('service'), `${text('date')} / ${text('time')}`, text('status'), text('actions')], rows)}</div>`); wireAppointmentControls(); } catch (error) { renderShell(text('appointments'), `<div class="error-state">${esc(normalizeError(error))}</div>`); } }
  function appointmentForm(item = {}) { return `${field(text('name'), 'patient_name', item.patient_name, 'text', true)}${field(text('phone'), 'phone', item.phone, 'tel', true)}${field(text('email'), 'email', item.email, 'email')}${field(text('service'), 'service', item.service)}${field(text('date'), 'preferred_date', item.preferred_date, 'date')}${field(text('time'), 'preferred_time', item.preferred_time, 'time')}${area(text('notes'), 'notes', item.notes)}<label>${text('status')}<select name="status"><option value="new" ${item.status === 'new' ? 'selected' : ''}>${text('new')}</option><option value="pending" ${item.status === 'pending' ? 'selected' : ''}>${text('pending')}</option><option value="confirmed" ${item.status === 'confirmed' ? 'selected' : ''}>${text('confirmed')}</option><option value="completed" ${item.status === 'completed' ? 'selected' : ''}>${text('completed')}</option><option value="cancelled" ${item.status === 'cancelled' ? 'selected' : ''}>${text('cancelled')}</option></select></label>`; }
  function wireAppointmentControls() { $('#add-appointment').addEventListener('click', () => openAppointment()); $('#module-search').addEventListener('input', filterAppointments); $('#status-filter').addEventListener('change', filterAppointments); $('#date-filter').addEventListener('change', filterAppointments); $$('[data-edit-appointment]').forEach((button) => button.addEventListener('click', () => openAppointment(state.cache.appointments.find((item) => item.id === button.dataset.editAppointment)))); wireDeleteButtons(); }
  function filterAppointments() { const search = $('#module-search').value.toLowerCase(); const status = $('#status-filter').value; const date = $('#date-filter').value; const data = state.cache.appointments.filter((item) => (!status || item.status === status) && (!date || item.preferred_date === date) && (!search || `${item.patient_name} ${item.phone} ${item.service}`.toLowerCase().includes(search))); const rows = data.map((item) => `<tr><td>${esc(item.patient_name)}</td><td>${esc(item.phone)}</td><td>${esc(item.service || '-')}</td><td>${esc(item.preferred_date || '-')} ${esc(item.preferred_time || '')}</td><td>${statusBadge(item.status)}</td><td><div class="row-actions"><button class="small-button" data-edit-appointment="${item.id}">${text('details')}</button><button class="small-button danger" data-delete="appointments" data-id="${item.id}">${text('delete')}</button></div></td></tr>`).join(''); $('#module-table').innerHTML = tableView([text('name'), text('phone'), text('service'), `${text('date')} / ${text('time')}`, text('status'), text('actions')], rows); wireAppointmentControls(); }
  function openAppointment(item) { const dialog = modal(item ? text('edit') : text('add'), appointmentForm(item)); dialog.querySelector('#modal-form').addEventListener('submit', async (event) => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget)); try { validateValues(values); await save('appointments', item?.id, values); dialog.remove(); toast(text('saved')); renderAppointments(); } catch (error) { toast(normalizeError(error), true); } }); }

  async function renderPatients() { loadingView(); try { const [data, appointments] = await Promise.all([query('patients', { order: 'created_at' }), query('appointments', { order: 'preferred_date' })]); state.cache.patients = data; state.cache.patientAppointments = appointments; const rows = data.map((item) => { const history = appointments.filter((appointment) => appointment.patient_id === item.id || appointment.phone === item.phone); const latest = history[history.length - 1]; return `<tr><td>${esc(item.name)}</td><td>${esc(item.phone)}</td><td>${esc(item.email || '-')}</td><td>${history.length}</td><td>${esc(latest?.preferred_date || '-')}</td><td><div class="row-actions"><button class="small-button" data-history="${item.id}">${text('details')}</button><button class="small-button" data-edit="patients" data-id="${item.id}">${text('edit')}</button><button class="small-button danger" data-delete="patients" data-id="${item.id}">${text('delete')}</button></div></td></tr>`; }).join(''); renderShell(text('patients'), `<div class="toolbar"><input id="module-search" placeholder="${esc(text('search'))}"><button class="button button-primary" id="add-record">+ ${text('add')}</button></div><div id="module-table">${tableView([text('name'), text('phone'), text('email'), text('appointments'), text('latest'), text('actions')], rows)}</div>`); $('#add-record').addEventListener('click', () => openCrud('patients', null, patientForm)); $$('[data-edit="patients"]').forEach((button) => button.addEventListener('click', () => openCrud('patients', data.find((item) => item.id === button.dataset.id), patientForm))); $$('[data-history]').forEach((button) => button.addEventListener('click', () => showPatientHistory(button.dataset.history))); $('#module-search').addEventListener('input', (event) => { const value = event.target.value.toLowerCase(); $$('#module-table tbody tr').forEach((row) => { row.hidden = !row.textContent.toLowerCase().includes(value); }); }); wireDeleteButtons(); } catch (error) { renderShell(text('patients'), `<div class="error-state">${esc(normalizeError(error))}</div>`); } }
  function patientForm(item = {}) { return `${field(text('name'), 'name', item.name, 'text', true)}${field(text('phone'), 'phone', item.phone, 'tel', true)}${field(text('email'), 'email', item.email, 'email')}${field('Date of birth', 'date_of_birth', item.date_of_birth, 'date')}${area(text('notes'), 'notes', item.notes)}`; }
  async function renderCrudList(table, headers, cells, formFactory, extraActions = () => '') { loadingView(); try { const data = await query(table, { order: 'created_at' }); state.cache[table] = data; const rows = data.map((item) => `<tr>${cells(item)}<td><div class="row-actions">${extraActions(item)}<button class="small-button" data-edit="${table}" data-id="${item.id}">${text('edit')}</button><button class="small-button danger" data-delete="${table}" data-id="${item.id}">${text('delete')}</button></div></td></tr>`).join(''); renderShell(text(table), `<div class="toolbar"><input id="module-search" placeholder="${esc(text('search'))}"><button class="button button-primary" id="add-record">+ ${text('add')}</button></div><div id="module-table">${tableView(headers, rows)}</div>`); $('#add-record').addEventListener('click', () => openCrud(table, null, formFactory)); $$('[data-edit="' + table + '"]').forEach((button) => button.addEventListener('click', () => openCrud(table, state.cache[table].find((item) => item.id === button.dataset.id), formFactory))); $$('[data-history]').forEach((button) => button.addEventListener('click', () => showPatientHistory(button.dataset.history))); $('#module-search').addEventListener('input', (event) => { const value = event.target.value.toLowerCase(); $$('#module-table tbody tr').forEach((row) => { row.hidden = !row.textContent.toLowerCase().includes(value); }); }); wireDeleteButtons(); } catch (error) { renderShell(text(table), `<div class="error-state">${esc(normalizeError(error))}</div>`); } }
  function openCrud(table, item, formFactory) { const dialog = modal(item ? text('edit') : text('add'), formFactory(item || {})); dialog.querySelector('#modal-form').addEventListener('submit', async (event) => { event.preventDefault(); const formData = new FormData(event.currentTarget); const values = Object.fromEntries(formData); ['is_active'].forEach((key) => { if (key in values) values[key] = values[key] === 'true'; }); ['price', 'duration', 'sort_order'].forEach((key) => { if (key in values && values[key] === '') values[key] = null; }); try { validateValues(values); const upload = formData.get('image_file'); if (upload?.size && ['gallery', 'services'].includes(table)) values.image_path = await uploadMedia(upload, table === 'gallery' ? 'gallery' : 'services'); delete values.image_file; await save(table, item?.id, values); dialog.remove(); toast(text('saved')); navigate(table); } catch (error) { toast(normalizeError(error), true); } }); }
  function wireDeleteButtons() { $$('[data-delete]').forEach((button) => button.addEventListener('click', async () => { if (!window.confirm(text('confirmDelete'))) return; try { await remove(button.dataset.delete, button.dataset.id); toast(text('deleted')); navigate(state.section); } catch (error) { toast(normalizeError(error), true); } })); }
  async function showPatientHistory(patientId) { try { const patient = state.cache.patients.find((item) => item.id === patientId); const history = await query('appointments', { order: 'preferred_date', filters: { patient_id: patientId } }); const rows = history.map((item) => `<tr><td>${esc(item.preferred_date || '-')}</td><td>${esc(item.service || '-')}</td><td>${statusBadge(item.status)}</td></tr>`).join(''); const dialog = modal(`${text('details')}: ${patient?.name || ''}`, tableView([text('date'), text('service'), text('status')], rows), text('cancel')); dialog.querySelector('#modal-form').remove(); } catch (error) { toast(normalizeError(error), true); } }

  async function renderServices() { await renderCrudList('services', [text('name'), text('price'), text('duration'), text('status'), text('actions')], (item) => `<td>${esc(state.lang === 'ar' ? item.name_ar : item.name_en)}</td><td>${esc(item.price ?? '-')}</td><td>${esc(item.duration || '-')}</td><td>${item.is_active ? statusBadge('active') : statusBadge('inactive')}</td>`, serviceForm); }
  function serviceForm(item = {}) { return `${field('Arabic name', 'name_ar', item.name_ar, 'text', true)}${field('English name', 'name_en', item.name_en, 'text', true)}${area('Arabic description', 'description_ar', item.description_ar)}${area('English description', 'description_en', item.description_en)}${field(text('price'), 'price', item.price, 'number')}${field(text('duration'), 'duration', item.duration, 'number')}${field(text('image'), 'image_path', item.image_path)}${imagePreview(item.image_path)}${mediaField(text('upload'))}${field('Sort order', 'sort_order', item.sort_order || 0, 'number')}<label>${text('active')}<select name="is_active"><option value="true" ${item.is_active !== false ? 'selected' : ''}>${text('active')}</option><option value="false" ${item.is_active === false ? 'selected' : ''}>${text('inactive')}</option></select></label>`; }
  async function renderGallery() { await renderCrudList('gallery', [text('image'), text('title'), text('category'), text('status'), text('actions')], (item) => `<td>${esc(item.image_path)}</td><td>${esc(state.lang === 'ar' ? item.title_ar : item.title_en)}</td><td>${esc(item.category || '-')}</td><td>${item.is_active ? statusBadge('active') : statusBadge('inactive')}</td>`, galleryForm); }
  function galleryForm(item = {}) { return `${field(text('image'), 'image_path', item.image_path, 'text', !item.id)}${imagePreview(item.image_path)}${mediaField(text('upload'))}${field('Arabic title', 'title_ar', item.title_ar)}${field('English title', 'title_en', item.title_en)}${field(text('category'), 'category', item.category)}${field('Sort order', 'sort_order', item.sort_order || 0, 'number')}<label>${text('active')}<select name="is_active"><option value="true" ${item.is_active !== false ? 'selected' : ''}>${text('active')}</option><option value="false" ${item.is_active === false ? 'selected' : ''}>${text('inactive')}</option></select></label>`; }

  async function renderDoctor() { try { const data = await query('doctor_profile', { order: 'created_at', limit: 1 }); const item = data[0] || {}; renderShell(text('doctorProfile'), `<form id="profile-form" class="entity-form">${field('Arabic name', 'name_ar', item.name_ar || 'عبدالرحمن محب', 'text', true)}${field('English name', 'name_en', item.name_en || 'Abdurahman Moheb', 'text', true)}${area('Arabic biography', 'bio_ar', item.bio_ar)}${area('English biography', 'bio_en', item.bio_en)}${area('Arabic credentials', 'credentials_ar', item.credentials_ar)}${area(text('qualifications'), 'credentials_en', item.credentials_en)}${area(text('fellowship'), 'fellowship_en', item.fellowship_en)}${field(text('image'), 'image_path', item.image_path || 'assets/images/doctor/doctor.jpg')}${imagePreview(item.image_path || 'assets/images/doctor/doctor.jpg')}${mediaField(text('upload'))}${field(text('instagram'), 'instagram_url', item.instagram_url)}<div class="form-actions"><button class="button button-primary" type="submit">${text('save')}</button></div></form>`); $('#profile-form').addEventListener('submit', async (event) => { event.preventDefault(); try { const formData = new FormData(event.currentTarget); const values = Object.fromEntries(formData); const upload = formData.get('image_file'); const path = await uploadMedia(upload, 'doctor'); if (path) values.image_path = path; delete values.image_file; validateValues(values); await save('doctor_profile', item.id, values); toast(text('saved')); } catch (error) { toast(normalizeError(error), true); } }); } catch (error) { renderShell(text('doctorProfile'), `<div class="error-state">${esc(normalizeError(error))}</div>`); } }
  async function renderSettings() { try { const data = await query('clinic_settings', { order: 'created_at', limit: 1 }); const item = data[0] || {}; renderShell(text('clinicSettings'), `<form id="settings-form" class="entity-form">${field('Arabic clinic name', 'clinic_name_ar', item.clinic_name_ar || 'عيادة الدكتور عبدالرحمن محب', 'text', true)}${field('English clinic name', 'clinic_name_en', item.clinic_name_en || 'Dr. Abdurahman Moheb Dental Clinic', 'text', true)}${field(text('email'), 'email', item.email, 'email')}${field(text('phone'), 'phone', item.phone, 'tel')}${field('WhatsApp', 'whatsapp', item.whatsapp, 'tel')}${field(text('googleMaps'), 'google_maps_url', item.google_maps_url, 'url')}${area('العنوان العربي', 'address_ar', item.address_ar)}${area('English address', 'address_en', item.address_en)}${area('ساعات العمل بالعربية', 'working_hours_ar', item.working_hours_ar)}${area('Working hours', 'working_hours_en', item.working_hours_en)}${field(text('instagram'), 'instagram_url', item.instagram_url, 'url')}${field('Facebook', 'facebook_url', item.facebook_url, 'url')}<div class="form-actions"><button class="button button-primary" type="submit">${text('save')}</button></div></form>`); $('#settings-form').addEventListener('submit', async (event) => { event.preventDefault(); try { const values = Object.fromEntries(new FormData(event.currentTarget)); validateValues(values); await save('clinic_settings', item.id, values); toast(text('saved')); } catch (error) { toast(normalizeError(error), true); } }); } catch (error) { renderShell(text('clinicSettings'), `<div class="error-state">${esc(normalizeError(error))}</div>`); } }
  async function renderMessages() { loadingView(); try { const data = await query('contact_messages', { order: 'created_at' }); state.cache.contact_messages = data; const rows = data.map((item) => `<tr><td>${esc(item.name)}</td><td>${esc(item.email || item.phone || '-')}</td><td>${esc(item.subject || '-')}</td><td class="message-cell">${esc(item.message)}</td><td>${esc(new Date(item.created_at).toLocaleString())}</td><td>${statusBadge(item.status)}</td><td><div class="row-actions">${item.status === 'unread' ? `<button class="small-button" data-mark-read="${item.id}">${text('markRead')}</button>` : `<button class="small-button" data-mark-unread="${item.id}">${text('markUnread')}</button>`}${item.status !== 'archived' ? `<button class="small-button" data-archive="${item.id}">${text('archive')}</button>` : ''}<button class="small-button danger" data-delete="contact_messages" data-id="${item.id}">${text('delete')}</button></div></td></tr>`).join(''); renderShell(text('messages'), `<div class="toolbar"><input id="module-search" placeholder="${esc(text('search'))}"></div><div id="module-table">${tableView([text('name'), text('email'), text('subject'), text('message'), text('date'), text('status'), text('actions')], rows)}</div>`); $('#module-search').addEventListener('input', (event) => { const value = event.target.value.toLowerCase(); $$('#module-table tbody tr').forEach((row) => { row.hidden = !row.textContent.toLowerCase().includes(value); }); }); $$('[data-mark-read]').forEach((button) => button.addEventListener('click', async () => { try { await save('contact_messages', button.dataset.markRead, { status: 'read', read: true }); toast(text('saved')); renderMessages(); } catch (error) { toast(normalizeError(error), true); } })); $$('[data-mark-unread]').forEach((button) => button.addEventListener('click', async () => { try { await save('contact_messages', button.dataset.markUnread, { status: 'unread', read: false }); toast(text('saved')); renderMessages(); } catch (error) { toast(normalizeError(error), true); } })); $$('[data-archive]').forEach((button) => button.addEventListener('click', async () => { try { await save('contact_messages', button.dataset.archive, { status: 'archived', read: true }); toast(text('saved')); renderMessages(); } catch (error) { toast(normalizeError(error), true); } })); wireDeleteButtons(); } catch (error) { renderShell(text('messages'), `<div class="error-state">${esc(normalizeError(error))}</div>`); } }

  const renderers = { overview: renderOverview, appointments: renderAppointments, patients: renderPatients, services: renderServices, gallery: renderGallery, doctor: renderDoctor, settings: renderSettings, messages: renderMessages };
  async function navigate(section) { state.section = section; $$('.nav-item').forEach((item) => item.classList.toggle('active', item.dataset.section === section)); $('#sidebar').classList.remove('open'); await renderers[section](); }
  function wireDashboard() { $('#admin-email').textContent = state.user.email; $$('.nav-item').forEach((button) => button.addEventListener('click', () => navigate(button.dataset.section))); $('#menu-button').addEventListener('click', () => $('#sidebar').classList.toggle('open')); $('#logout-button').addEventListener('click', async () => { await state.client.auth.signOut(); toast(text('signedOut')); show('login-view'); }); navigate('overview'); show('dashboard-view'); }
  async function handleLogin(event) { event.preventDefault(); const form = event.currentTarget; const message = $('#login-message'); const submit = form.querySelector('button[type="submit"]'); message.hidden = true; submit.disabled = true; try { const { data, error } = await state.client.auth.signInWithPassword({ email: $('#login-email').value.trim(), password: $('#login-password').value }); if (error) throw error; if (!data.user || data.user.email.toLowerCase() !== ADMIN_EMAIL) { await state.client.auth.signOut(); throw new Error('Unauthorized'); } await authorizeUser(data.user); state.user = data.user; wireDashboard(); } catch (error) { if (error.message === 'Unauthorized' || error.code === 'PGRST116') { await state.client.auth.signOut(); } message.textContent = error.message === 'Unauthorized' || error.code === 'PGRST116' ? text('accessDenied') : text('loginFailed'); message.className = 'form-message error'; message.hidden = false; } finally { submit.disabled = false; } }
  async function authorizeUser(user) { const result = await state.client.from('admin_users').select('user_id').eq('user_id', user.id).maybeSingle(); if (result.error || !result.data) throw new Error('Unauthorized'); }
  async function start() { applyLanguage(); $$('[data-language-toggle]').forEach((button) => button.addEventListener('click', () => { state.lang = state.lang === 'ar' ? 'en' : 'ar'; localStorage.setItem('admin_lang', state.lang); applyLanguage(); if (!$('#dashboard-view').hidden) navigate(state.section); })); $('#login-form').addEventListener('submit', handleLogin); show('loading-view'); try { const env = await loadEnvironment(); state.config = { url: env.VITE_SUPABASE_URL, publishableKey: env.VITE_SUPABASE_PUBLISHABLE_KEY }; state.client = window.supabase.createClient(state.config.url, state.config.publishableKey, { auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: true } }); const { data, error } = await state.client.auth.getSession(); if (error) throw error; if (data.session && data.session.user.email.toLowerCase() === ADMIN_EMAIL) { await authorizeUser(data.session.user); state.user = data.session.user; wireDashboard(); } else { if (data.session) await state.client.auth.signOut(); show('login-view'); } state.client.auth.onAuthStateChange((_event, session) => { if (!session) show('login-view'); }); } catch (error) { show('configuration-view'); } }
  start();
})();
